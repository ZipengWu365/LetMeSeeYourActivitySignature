# Prepared Data

Place the following files here before running experiments:

- `X_feats.pkl` - Extracted features (N, 32) - ~200MB
- `Y_Walmsley2020.npy` - Activity labels - ~60MB  
- `P.npy` - Participant IDs - ~15MB

These files can be generated from CAPTURE-24 dataset using the feature extraction pipeline.
