# =====================================================
# Gait Filter Pipeline - Requirements
# =====================================================

# 核心依赖
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0
joblib>=1.3.0

# PyTorch (CUDA加速必需)
# 安装方式: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0

# Mamba SSM (可选，需要 CUDA 11.8+)
# 安装方式: pip install mamba-ssm causal-conv1d
# mamba-ssm>=1.0.0
# causal-conv1d>=1.1.0

# 可视化 (可选)
matplotlib>=3.7.0
seaborn>=0.12.0

# 统计分析
statsmodels>=0.14.0

# 进度条
tqdm>=4.65.0

# XGBoost (RF对比实验)
xgboost>=1.7.0

# =====================================================
# 安装命令 (推荐顺序):
# =====================================================
# 1. 基础依赖:
#    pip install numpy pandas scikit-learn scipy joblib tqdm matplotlib seaborn statsmodels xgboost
#
# 2. PyTorch with CUDA (ESN加速):
#    pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# 3. Mamba SSM (可选，需要Linux + CUDA 11.8+):
#    pip install mamba-ssm causal-conv1d>=1.1.0
# =====================================================
